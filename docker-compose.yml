version: '3.8'

services:
  creditfile-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./creditfile:/app/creditfile
      - ./app.py:/app/app.py
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_LOGGER_LEVEL=info
    restart: unless-stopped
